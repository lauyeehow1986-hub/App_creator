% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/target-tauri.R
\name{build_tauri}
\alias{build_tauri}
\title{Build the native shell (Tauri) target}
\usage{
build_tauri(
  app_dir,
  out_dir = "dist/tauri",
  backend = c("wasm", "portable"),
  platform = "windows",
  frontend_dist = NULL,
  app_name = NULL,
  identifier = NULL,
  run_build = TRUE,
  ...
)
}
\arguments{
\item{app_dir}{Path to a directory containing a runnable Shiny app
(an \code{app.R}, or \code{ui.R} + \code{server.R}).}

\item{out_dir}{Directory to write the generated project/build into.}

\item{backend}{One of \code{"wasm"} (implemented) or \code{"portable"}
(not yet implemented).}

\item{platform}{One or more of \code{"windows"} (default), \code{"macos"},
\code{"linux"} (validated; only the current host is actually built -
see Details). \code{"android"}/\code{"ios"} are not yet implemented.}

\item{frontend_dist}{Path to an already-built \code{\link[=build_wasm]{build_wasm()}} bundle
to wrap. If \code{NULL} (default), one is built automatically into
\code{out_dir/wasm-frontend}.}

\item{app_name}{Application name. If \code{NULL}, derived from \code{app_dir}.}

\item{identifier}{Reverse-DNS bundle identifier (e.g.
\code{"com.mycompany.myapp"}). Required to be non-default and unique
per Tauri - if \code{NULL}, derived from \code{app_name}.}

\item{run_build}{If \code{TRUE} (default), attempt to actually compile
the generated project. If no Tauri CLI is found, this degrades to
scaffolding only (with a warning), rather than failing.}

\item{...}{Reserved for future options.}
}
\value{
Invisibly, the build manifest (also written as
\code{manifest.json} inside \code{out_dir}).
}
\description{
Wraps a \code{\link[=build_wasm]{build_wasm()}} bundle in a native Tauri shell so the app
launches as a real double-click desktop app instead of "open this
HTML file in a browser". Tauri uses the OS's built-in webview
(WebView2 on Windows, WKWebView on macOS, WebKitGTK on Linux)
rather than bundling a browser engine, which is why the shell adds
only single-digit MB on top of the wasm payload (verified: a
minimal Tauri shell built from this package's own generator is
~9MB on Linux).
}
\section{What this does}{
\enumerate{
\item Builds (or reuses, via \code{frontend_dist}) a \code{\link[=build_wasm]{build_wasm()}} bundle
as the frontend.
\item Generates a \verb{src-tauri-project/} Rust/Tauri project around it
(\code{write_tauri_project()}) - a real, verified-to-compile Tauri 2
project, not a template that's only been read, not run.
\item If a Tauri CLI is available on the \emph{build} machine (\verb{cargo tauri}
or, failing that, \code{npx @tauri-apps/cli}), builds a bare
executable (\code{--no-bundle}: a self-contained binary, no installer
\itemize{
\item matches the "just copy it, no install" ethos of the whole
package; full per-OS installer packaging (.msi/.dmg/.deb) is a
possible future addition, not attempted here). If no CLI is
found, the project is left scaffolded and ready for \verb{cargo tauri build} to be run manually.
}
}

Only builds for the \emph{current host} platform - cross-compiling a
Tauri app for Windows/macOS from another OS needs additional
toolchains (\code{cargo-xwin}, \code{osxcross}) not wired up here. \code{platform}
is validated but not used to cross-compile; see
\code{docs/ARCHITECTURE.md}.

\code{backend = "portable"} (wrapping a \code{\link[=build_portable]{build_portable()}} bundle via a
Tauri sidecar process) and mobile platforms (\code{"android"}/\code{"ios"})
are designed but not implemented yet - both fail loudly rather than
silently doing the wrong thing.
}

