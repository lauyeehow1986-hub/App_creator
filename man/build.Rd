% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R
\name{build}
\alias{build}
\title{Build a portable, zero-admin-install bundle from a Shiny app}
\usage{
build(app_dir, targets = "wasm", out_dir = "dist", ...)
}
\arguments{
\item{app_dir}{Path to a directory containing a runnable Shiny app
(an \code{app.R}, or \code{ui.R} + \code{server.R}).}

\item{targets}{Character vector of targets to build. One or more of
\code{"wasm"}, \code{"portable"}, \code{"tauri"}. See \code{vignette("architecture")}
for the tradeoffs of each.}

\item{out_dir}{Directory to write build output into. One
subdirectory per target is created.}

\item{...}{Target-specific arguments, forwarded to the underlying
\verb{build_*()} function (e.g. \code{platform} for \code{"portable"}/\code{"tauri"}).}
}
\value{
Invisibly, a list of manifests (one per target), each as
returned by the corresponding \verb{build_*()} function.
}
\description{
Single entry point for every build target. Dispatches to
\code{\link[=build_wasm]{build_wasm()}}, \code{\link[=build_portable]{build_portable()}}, and/or \code{\link[=build_tauri]{build_tauri()}} and
returns one manifest per target.
}
