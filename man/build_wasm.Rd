% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/target-wasm.R
\name{build_wasm}
\alias{build_wasm}
\title{Build the pure-browser WebAssembly (shinylive/webR) target}
\usage{
build_wasm(
  app_dir,
  out_dir = "dist/wasm",
  serve_launcher = TRUE,
  check_deps = TRUE,
  ...
)
}
\arguments{
\item{app_dir}{Path to a directory containing a runnable Shiny app
(an \code{app.R}, or \code{ui.R} + \code{server.R}).}

\item{out_dir}{Directory to write the static bundle into.}

\item{serve_launcher}{If \code{TRUE} (default), write a \code{run.bat} /
\code{run.sh} launcher into \code{out_dir} that serves the bundle over
\verb{http://localhost} using whatever local static-server tooling is
already on the machine, sidestepping the \verb{file://} CORS/MIME
restrictions some browsers apply to WASM + service workers.}

\item{check_deps}{If \code{TRUE} (default), run \code{\link[=check_wasm_packages]{check_wasm_packages()}}
first and abort with a clear, actionable list if the app depends on
packages that can't run in a WebAssembly bundle (no webR binary, or
installed from GitHub) - instead of letting \code{shinylive::export()}
fail cryptically partway through. Set \code{FALSE} to skip the check.}

\item{...}{Passed through to \code{shinylive::export()}.}
}
\value{
Invisibly, the build manifest (also written as
\code{manifest.json} inside \code{out_dir}).
}
\description{
Compiles the app's R/Shiny code and pre-compiled WASM package
binaries into a static bundle that runs entirely inside the user's
existing browser — no R install, no server process, on any OS with
a modern browser (Windows/macOS/Linux/Android/iOS). This is the
default, smallest-footprint target; see \code{vignette("architecture")}
for where it breaks down (arbitrary compiled CRAN packages, native
filesystem/DB access).
}
\details{
Requires the \code{shinylive} package on the \emph{build} machine only (the
deployment target needs nothing). Run once with internet access so
\code{shinylive} can download/cache the webR runtime and package
binaries; subsequent builds reuse that cache.
}
