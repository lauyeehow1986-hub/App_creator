% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/target-portable.R
\name{build_portable}
\alias{build_portable}
\title{Build the portable-R backend target (Windows implemented)}
\usage{
build_portable(
  app_dir,
  out_dir = "dist/portable",
  platform = c("windows", "macos", "linux"),
  packages = NULL,
  r_portable_version = NULL,
  cache_dir = tools::R_user_dir("shinyalcatraz", "cache"),
  port = 8973,
  ...
)
}
\arguments{
\item{app_dir}{Path to a directory containing a runnable Shiny app
(an \code{app.R}, or \code{ui.R} + \code{server.R}).}

\item{out_dir}{Directory to write the portable bundle into.}

\item{platform}{One of \code{"windows"}, \code{"macos"}, \code{"linux"}. Only
\code{"windows"} is currently implemented.}

\item{packages}{Character vector of R package names to install into
the bundle. If \code{NULL} (default), auto-detected from \code{app_dir}'s R
files via \code{\link[=scan_r_package_deps]{scan_r_package_deps()}}.}

\item{r_portable_version}{R-Portable version to fetch, e.g.
\code{"4.2.0"}. If \code{NULL} (default), uses whatever
\code{sourceforge.net/projects/rportable/files/latest} currently
resolves to.}

\item{cache_dir}{Directory to cache the downloaded R-Portable build
in across calls. Defaults to a per-user cache dir (see
\code{\link[tools:userdir]{tools::R_user_dir()}}).}

\item{port}{Local port the launcher should use. Defaults to a fixed
port (8973); pass a different value if that's likely to collide
with something else on target machines.}

\item{...}{Reserved for future options.}
}
\value{
Invisibly, the build manifest (also written as
\code{manifest.json} inside \code{out_dir}).
}
\description{
Bundles a real portable R runtime plus the app's package
dependencies, with a \code{run.bat} launcher that starts the app via
\code{shiny::runApp()} and opens the default browser. Unlike
\code{\link[=build_wasm]{build_wasm()}}, this gives full CRAN compatibility (compiled
C/C++/Fortran packages, real filesystem/DB access) at the cost of a
much larger bundle (~150-300+MB depending on dependencies).
}
\section{How it works (\code{platform = "windows"})}{
\enumerate{
\item Dependencies are auto-detected from the app's R files (every
\code{library()}/\code{require()} call and every \code{pkg::fn()} usage) unless
\code{packages} is supplied explicitly.
\item A portable R runtime is downloaded from the
\href{https://sourceforge.net/projects/rportable/}{R-Portable} project
(a PortableApps NSIS package) and cached in \code{cache_dir}, so
repeat builds don't re-download it.
\item Dependencies are installed into a private library inside the
bundle using the \emph{bundled} Rscript.exe (not the build machine's
R), so the installed binaries match the shipped R version/ABI.
Each dependency is routed by where the build machine got it:
CRAN packages install as Windows binaries; packages from a git
forge or URL (GitHub/GitLab/Bitbucket/generic git/tarball URL, per
the \code{RemoteType} in their \code{DESCRIPTION}) reinstall via the matching
\verb{remotes::install_*()}; Bioconductor packages (a \code{biocViews} field)
from the Bioconductor repos; and packages from a custom CRAN-like
repo (r-universe, Posit Package Manager) via that repo's URL from
their \code{Repository} field - all run by the bundled R so the binaries
match its ABI. Anything a repo still can't provide but that's
installed and pure-R on the build machine is copied in. After
install, the bundle library is checked and any package that failed
to install is reported, rather than silently shipping a bundle
that crashes on the target. The build machine needs internet for
this step; the resulting bundle does not.
\item A \code{run.bat} launcher sets \code{R_LIBS} to the bundle's private
library (never the host's), then calls the bundled Rscript.exe
to run \code{shiny::runApp(launch.browser = TRUE)}.
\item \code{manifest.json} is written via the same \code{write_build_manifest()}
every target uses, for version-stamping.
}

\code{platform = "macos"}/\code{"linux"} are not implemented yet (R-Portable
is Windows-only; macOS/Linux would need a statically-linked R build,
e.g. via rig or conda-forge, not yet evaluated - see
\code{docs/ARCHITECTURE.md}).
}

