% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update-check.R
\name{enable_update_check}
\alias{enable_update_check}
\title{Add an opportunistic "update available" banner to a built bundle}
\usage{
enable_update_check(bundle_dir, version_url, timeout_ms = 1500)
}
\arguments{
\item{bundle_dir}{Path to a bundle directory previously produced by
\code{\link[=build_wasm]{build_wasm()}} (must contain \code{index.html} and \code{manifest.json}).}

\item{version_url}{URL of a small JSON endpoint your team controls
that returns \verb{\{"build_sha": "...", "package_version": "..."\}"}
for the latest build. Never called at build time - only from the
deployed app itself, and only if that machine happens to have a
route to it.}

\item{timeout_ms}{How long the banner check waits before giving up
silently. Default 1500ms - short enough that a fully offline
machine never notices a delay.}
}
\value{
Invisibly, \code{bundle_dir}.
}
\description{
Injects a small vanilla-JS snippet into a \code{\link[=build_wasm]{build_wasm()}} bundle's
\code{index.html} that, on page load, tries a single short-timeout fetch
to \code{version_url}. If that succeeds and reports a newer build than
this bundle's own \code{manifest.json}, a small dismissible banner is
shown. If the machine is offline, or \code{version_url} is unreachable,
or the request errors for any reason, the check fails \strong{silently}
\itemize{
\item no banner, no console noise, no delay to app startup. This is
opt-in and deliberately dumb: it never auto-updates anything, it
only tells whoever's looking at the screen that a newer copy
exists somewhere reachable.
}
}
\details{
Must be called \emph{after} the target bundle has been built (so its
\code{manifest.json} and \code{index.html} already exist).
}
