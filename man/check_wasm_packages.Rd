% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/target-wasm.R
\name{check_wasm_packages}
\alias{check_wasm_packages}
\title{Check an app's package dependencies for WebAssembly compatibility}
\usage{
check_wasm_packages(app_dir, r_versions = c("4.5", "4.4", "4.3"))
}
\arguments{
\item{app_dir}{Directory containing the Shiny app.}

\item{r_versions}{webR repo R-version shelves to check (unioned).}
}
\value{
Invisibly, a list: \code{no_wasm_build}, \code{from_github},
\code{github_but_wasm_available} (character vectors), \code{pulled_by} (named
vector mapping each blocker to the direct dep that pulls it, or \code{""}
if it is itself a direct dep), and \code{checked} (\code{FALSE} if the repo
was unreachable).
}
\description{
A pre-flight for \code{\link[=build_wasm]{build_wasm()}}. It statically detects the packages an
app uses (via \code{\link[=scan_r_package_deps]{scan_r_package_deps()}}), expands their recursive
dependency tree, and checks each against the webR package repository
(\code{repo.r-wasm.org}), flagging two things that otherwise only surface
as a confusing failure partway through \code{shinylive::export()}:
}
\details{
\itemize{
\item \strong{No WebAssembly build} - packages (or their dependencies) with no
binary in the webR repo at all. These cannot run in a browser-only
bundle and must be removed or swapped for a wasm-available
alternative. Transitive blockers are reported with the direct
dependency that pulls them in (e.g. \verb{websocket (via webshot2)}).
\item \strong{Installed from GitHub} - packages whose local install metadata
points at a GitHub repo. \code{shinylive} will try to fetch a matching
GitHub \emph{release} for them and fail if none exists (the common
\code{get_github_wasm_assets()} 404). If such a package is \emph{also} in the
webR repo, reinstalling it from CRAN (or clearing its
\verb{Remote*}/\verb{Github*} \code{DESCRIPTION} fields) is enough; if not, it has
to go.
}

Availability is checked against several recent R minor-version shelves
and unioned, so it stays correct as webR's bundled R version moves.
The check is fail-soft: if the webR repo can't be reached it warns and
returns rather than blocking a build.
}
