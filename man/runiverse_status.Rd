% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/target-portable.R
\name{runiverse_status}
\alias{runiverse_status}
\title{Check r-universe build status and/or wasm-bundle presence for an app's remotes}
\usage{
runiverse_status(app_dir, universe = NULL, bundle = NULL)
}
\arguments{
\item{app_dir}{Directory containing the Shiny app.}

\item{universe}{Your r-universe URL, e.g. \code{"https://you.r-universe.dev"}.
When given, checks each package is available there (i.e. r-universe has
built it - the WebAssembly target is built alongside, so also confirm the
Emscripten column on your universe dashboard).}

\item{bundle}{A built \code{\link[=build_wasm]{build_wasm()}} output directory; when given, checks
each package's \code{.tgz} is present under \verb{shinylive/webr/packages/}.}
}
\value{
Invisibly, a data frame with \code{package}, \code{on_universe}, \code{in_bundle}
(\code{NA} where not checked).
}
\description{
Wraps the two verification steps of the r-universe path for \code{\link[=build_wasm]{build_wasm()}}:
(step 4) whether each of an app's git-forge packages is being served by your
r-universe yet, and (step 7) whether each one actually landed in a built wasm
bundle. Pass \code{universe}, \code{bundle}, or both.
}
\seealso{
\code{\link[=runiverse_registry]{runiverse_registry()}}, \code{\link[=write_runiverse_registry]{write_runiverse_registry()}}
}
